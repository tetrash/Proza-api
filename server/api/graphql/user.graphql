enum UserRole {
  admin
  moderator
  user
}

type User {
  id: String
  username: String
  fullName: String
  email: String
  openid: String
  role: UserRole
  avatarUrl: String
  createdAt: String
  updatedAt: String
}

type Query {
  me: User
}